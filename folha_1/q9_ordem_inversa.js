//9. Leia 2 números (A, B) e escreva-os em ordem inversa (B, A).
const input = require('prompt-sync')()

// Entrada
const numero_a = Number(input('Entre com um número: '))
const numero_b = Number(input('Mais uma vez: '))

// Processamento
    //Não há muito o que processar...
// Saída
console.log('Em ordem inversa eles ficam da seguinte maneira: ', numero_b, numero_a)